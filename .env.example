# The SPA talks ONLY to the deployed backend. Set this to the LIVE backend base
# URL (no trailing slash), e.g. https://queuestorm-backend.onrender.com
# Local backend is intentionally NOT the default — Phase 0 verifies against LIVE.
VITE_API_BASE_URL=https://your-backend.onrender.com
