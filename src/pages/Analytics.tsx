import StubPage from "../components/StubPage";

export default function Analytics() {
  return (
    <StubPage
      title="Analytics"
      subtitle="GET /analytics/summary, /patterns, /timeline — charts via Recharts."
      phase="Phase 3"
    />
  );
}
