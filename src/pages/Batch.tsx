import StubPage from "../components/StubPage";

export default function Batch() {
  return (
    <StubPage
      title="Batch"
      subtitle="POST /analyze-batch — up to 20 tickets with per-item partial success."
      phase="Phase 3"
    />
  );
}
