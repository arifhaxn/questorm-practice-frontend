import StubPage from "../components/StubPage";

export default function Analyze() {
  return (
    <StubPage
      title="Analyze"
      subtitle="POST /analyze-ticket — submit a single ticket and view the AI verdict."
      phase="Phase 1"
    />
  );
}
